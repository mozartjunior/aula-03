# 🚀 API RESTful com Node.js + Express

Projeto inicial para criar uma API RESTful utilizando **Node.js**, **Express** e padrões REST para comunicação via HTTP.

---

## 📦 Instalação

Clone o repositório e instale as dependências:

```bash
git clone https://github.com/seu-usuario/seu-repositorio.git
cd seu-repositorio
npm install
